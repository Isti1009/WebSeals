import React from "react";

const Hero = () => {
  return (
    <section
      className="h-screen bg-cover bg-center"
      style={{
        backgroundImage: "url('/images/hero.png')",
      }}
    ></section>
  );
};

export default Hero;
